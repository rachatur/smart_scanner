# Web framework
Django==5.0.6

# WSGI server
# gunicorn==20.1.0

# Database
# mysqlclient==2.1.0  # MySQL database adapter for Python

# Django REST framework
djangorestframework==3.15.1

# Environment variables management
# python-dotenv==0.20.0

# Security
cryptography==42.0.5

# HTTP requests
requests==2.31.0

# YAML parsing
pyyaml==6.0.1

# Additional libraries
datetime  # Part of Python standard library, no need to install
# os  # Part of Python standard library, no need to install
json  # Part of Python standard library, no need to install
logging  # Part of Python standard library, no need to install

# Django extensions and additional libraries
#django-environ==0.9.0  # For environment variable management in Django
#django-cors-headers==3.10.0  # For handling CORS headers in Django
#djangorestframework-simplejwt==5.1.0  # For JWT authentication with Django REST framework has context menu has context menu
